# ptero

## Install

Run the installer as root. It downloads the binary for your architecture, verifies its checksum, and installs it to `/usr/local/bin/ptero`:

```bash
curl -sSL https://ptero.2hoch1.dev/get.sh | sudo bash
```

Then get started:

```bash
ptero init
```
